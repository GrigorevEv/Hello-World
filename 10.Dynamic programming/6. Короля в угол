# Упражнение №6
# Реализовать алгоритм поиска выигрышных и проигрышных позиций в аналогичной игре,
# но ходы делает король (только вправо, вниз и по диагонали).


def king_in_the_corner(n, m):

    a = [[2]*m for i in range(n)]  # создаем двумерный массив
    a[n-1][m-1] = 0                # заполняем последнюю клетку

    for i in range(n-2, -1, -1):   # заполняем последнюю вертикаль
        if a[i+1][m-1] == 0:
            a[i][m-1] = 1
        else:
            a[i][m-1] = 0

    for j in range(m-2, -1, -1):   # заполняем последнюю горизонталь
        if a[n-1][j+1] == 0:
            a[n-1][j] = 1
        else:
            a[n-1][j] = 0

    for i in range(n-2, -1, -1):   # заполняем массив
        for j in range(m-2, -1, -1):
            if a[i+1][j+1] == 0 or a[i+1][j] == 0 or a[i][j+1] == 0:
                a[i][j] = 1
            else:
                a[i][j] = 0

    for i in range(n):
        print(a[i])


king_in_the_corner(15,20)





